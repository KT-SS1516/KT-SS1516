%% Corrects a given message 'y' using the error syndrome 's'.
%% Returns: corrected message 'x', number of corrected errors 'n' and
%% boolean 'b' if more errors were detected
function [x, n, b] = correct(y, s)

end

